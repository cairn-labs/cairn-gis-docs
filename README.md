# Cairn GIS Documentation Source

## Installation
Install mkdocs with 

    pip install mkdocs

## Usage
In the `cairn-gis-docs` directory, run `mkdocs serve`. Go to `localhost:8000`. Lo and behold, docs!
